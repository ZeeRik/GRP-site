<tr>
    <td>
        <div class="input-group">
            <span class="input-group-addon">
                {title}
            </span>
            <input type="{type}" name="{name}" class="form-control" value="{value}" placeholder="Введите: {title}">
        </div>
    </td>
</tr>